# Turso (libSQL) database
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-token

# IP hashing (RGPD) — 64 chars hex, required in production
IP_HASH_SECRET=generate-with-openssl-rand-hex-32

# Admin endpoints protection
ADMIN_KEY=your-admin-key

# Football-Data.org API (cron matchs)
FOOTBALL_DATA_API_KEY=your-api-key

# CORS origins (comma-separated, production)
CORS_ORIGINS=https://www.topflop.fr,https://topflop.fr,https://frontend-xtorbis-projects.vercel.app
